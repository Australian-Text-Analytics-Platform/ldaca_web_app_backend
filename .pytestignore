# Prevent pytest from recursing into bundled runtimes and dependency caches

# These directories are generated by the desktop (Tauri) build and contain third-party

# libraries with their own test suites. Keeping them out of discovery avoids thousands of

# irrelevant errors when running `pytest .`.

\*/site-packages/
